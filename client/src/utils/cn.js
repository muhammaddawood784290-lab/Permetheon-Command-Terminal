// =====================================================================
// cn — utility for conditional className composition
// =====================================================================

import clsx from 'clsx';

export function cn(...args) {
  return clsx(...args);
}
