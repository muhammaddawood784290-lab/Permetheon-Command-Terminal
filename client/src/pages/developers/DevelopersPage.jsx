// =====================================================================
// DevelopersPage — placeholder (alias of /users for developers list).
// =====================================================================

import PageContainer from '../../layouts/PageContainer';

export default function DevelopersPage() {
  return (
    <PageContainer title="Developers" subtitle="Browse the developer directory">
      <div className="bg-bg-surface border border-bg-elevated rounded-lg p-6 text-sm text-text-muted">
        Developer directory coming soon.
      </div>
    </PageContainer>
  );
}
