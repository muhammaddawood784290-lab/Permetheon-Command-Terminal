// =====================================================================
// TasksListPage — placeholder.
// =====================================================================

import PageContainer from '../../layouts/PageContainer';

export default function TasksListPage() {
  return (
    <PageContainer title="Tasks" subtitle="All tasks across projects">
      <div className="bg-bg-surface border border-bg-elevated rounded-lg p-6 text-sm text-text-muted">
        Tasks list coming in the next pass.
      </div>
    </PageContainer>
  );
}
