// =====================================================================
// RolesPage — placeholder.
// =====================================================================

import PageContainer from '../../layouts/PageContainer';

export default function RolesPage() {
  return (
    <PageContainer title="Roles & Permissions" subtitle="Manage access control">
      <div className="bg-bg-surface border border-bg-elevated rounded-lg p-6 text-sm text-text-muted">
        Roles and permissions matrix coming soon.
      </div>
    </PageContainer>
  );
}
