// =====================================================================
// ProjectsListPage — placeholder.
// =====================================================================

import PageContainer from '../../layouts/PageContainer';

export default function ProjectsListPage() {
  return (
    <PageContainer title="Projects" subtitle="Browse and manage active projects">
      <div className="bg-bg-surface border border-bg-elevated rounded-lg p-6 text-sm text-text-muted">
        Projects list view — coming in the next implementation pass.
      </div>
    </PageContainer>
  );
}
