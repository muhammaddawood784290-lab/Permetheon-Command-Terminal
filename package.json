{
  "name": "pct",
  "version": "1.0.0",
  "description": "PCT - Permetheon Command Terminal",
  "private": true,
  "type": "module",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm --workspace client run dev",
    "build": "npm --workspace client run build",
    "preview": "npm --workspace client run preview"
  }
}
